Slide-in panel from the right for per-day long-tail fields (notes, GPX links, resupply info) that don't fit the grid.

```jsx
<Drawer title="Day 4 — Col de la Croix" subtitle="Tue, Sep 8"><Input label="GPX link" /></Drawer>
```

Fixed 380px width at desktop; on tablet widths it should widen to ~100% minus a small margin rather than shrink content.
