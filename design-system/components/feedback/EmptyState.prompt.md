Used for module list-screens with no rows yet (accommodation, logistics, packing, expenses, sights).

```jsx
<EmptyState title="No accommodation yet" description="Add a place to stay for each night of the trip." actionLabel="Add accommodation" />
```

Dashed border (not a solid card) signals "not-yet-populated" distinctly from real content cards.
