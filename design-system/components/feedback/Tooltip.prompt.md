Fixed dark tooltip, always shown open here for spec purposes (real usage toggles on hover/focus).

```jsx
<Tooltip label="Elevation gain, in meters"><span>Elev. +1,240m</span></Tooltip>
```

Tooltip color is intentionally theme-independent (always dark-on-light) so it stays legible over any surface in either theme.
